// Runtime configuration for the static (GitHub Pages) deployment.
//
// _API_HOST is the origin of the backend, e.g. "https://knowingant-shengji.fly.dev".
// An empty string means "same origin as this page" (used when the backend
// serves the frontend itself; the backend overrides this file in that case).
window._API_HOST = "https://knowingant-shengji.fly.dev";
window._VERSION = "github-pages";
